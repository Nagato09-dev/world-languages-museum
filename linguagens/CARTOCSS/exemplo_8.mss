#layer {
  // Tratamento de Erros (não aplicável diretamente em CartoCSS, foca na validação)
  // Erros geralmente são reportados pelo parser ou editor.
  // Exemplo de sintaxe válida para evitar erros:
  line-color: #F00;
  line-width: 1;
}
