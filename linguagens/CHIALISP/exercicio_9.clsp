(mod (base exponent)
  ; Potência simples (base^exponent) - para inteiros pequenos
  (defun power (b e res)
    (if (= e 0)
        res
        (power b (- e 1) (* res b))
    )
  )
  (power base exponent 1)
)
