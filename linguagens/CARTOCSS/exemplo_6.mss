#layer {
  // Funções (simulado com mixins ou expressões)
  // CartoCSS não tem funções no sentido tradicional, mas permite expressões
  polygon-fill: rgb(255, 0, 0);
  line-color: hsl(120, 100%, 50%);
}
